<!DOCTYPE html>
<html>
  <head>
    <title>HackathonBlog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>


  <body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light container">
    <div class="row">
        <div class="col"><%= link_to 'Home', root_path, class: 'btn btn-info' %></div>
        <div class="col"><%= link_to 'New', new_path, class: 'btn btn-info' %></div>
        <% if user_signed_in? %>
		<span class="nav-greeting"> <%= "Welcome back, #{current_user.email}" %> </span>
		<div class="col-auto"><%= link_to "Sign Out", destroy_user_session_path, method: "Delete", class: 'btn btn-info' %></div>
	<% else %>
		<div class="col-auto"><%= link_to "Sign In", new_user_session_path, class: 'btn btn-info' %></div>
		<div class="col-auto"><%= link_to "Sign Up", new_user_registration_path, class: 'btn btn-info' %></div>
	<% end %>
    </div>
  </nav>
    <div class="container">
    
    <% if not notice.nil? %>
     <p class="alert alert-success"><%= notice %></p>
    <% elsif not alert.nil? %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>

    <%= yield %>
    </div>
  </body>
</html>
